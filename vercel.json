{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bunx turbo build --filter=@comp/app && cp -r apps/app/.next .next && cp -r apps/app/public public 2>/dev/null || true",
  "installCommand": "bun install",
  "outputDirectory": ".next"
}
